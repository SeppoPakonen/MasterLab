# Cute Edit Range Form Layout

- Root window: `TopWindow` titled `Cute Edit Range`
- Primary controls: range selector, start frame, end frame
- Footer actions: `OK`, `Cancel`
- Phase 1 note: preserves qtractor range dialog intent without binding to runtime session state yet

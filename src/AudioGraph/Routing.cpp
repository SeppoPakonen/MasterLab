#include "AudioGraph.h"
namespace am {
void Routing::Process(Graph& g, AudioBuffer& io) {
    // Placeholder for graph execution (toposort etc.)
    // This is where the actual routing and processing would happen
}
}